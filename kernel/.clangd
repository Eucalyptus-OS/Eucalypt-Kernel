# clangd tweaks that apply on top of compile_commands.json (generated by
# `make -C kernel`, no bear needed). Keep flag changes out of here — the
# database from the real build is the source of truth.

Diagnostics:
  # Kernel headers are shared by many translation units; unused-includes
  # warnings would fire constantly.
  UnusedIncludes: None
